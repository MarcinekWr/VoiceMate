[pytest]
minversion = 6.0
addopts = -ra --cov=src --cov-report=term-missing --cov-report=html
testpaths =
    tests
python_files = *_test.py
                   
python_functions = test_*

# Ignore deprecation warnings, which can be noisy
filterwarnings =
    ignore::DeprecationWarning
